$(document).ready(function () {

    //$('#loaderbody').addClass('hide');

    //var Popup;

    //ActiveDataTable();
    //AjaxEventNotifications();


    //$(document).on('change', '.status-dropdown', function () {
    //    var selectedStatus = $(this).find('option:selected').text();
    //    var Url = $(this).attr('data-url');
    //    var jobNumber = $(this).attr('data-jobNumber');
    //    var selectedDropdown = $(this).attr('data-name');

    //    StatusChangeUpdate(selectedStatus, Url, jobNumber, selectedDropdown);

    //});

});